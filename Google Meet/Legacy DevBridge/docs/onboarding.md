# Onboarding

The planned path is: install, connect Google, connect GitHub, detect an Apps Script project, create or select a repository, choose a standards preset, and become ready. The extension will derive and display the next step from typed backend onboarding state. Missing credentials use local mocks in development; no successful connection is simulated.
