# Apps Script integration

The Apps Script adapter planned for Milestone 4 will expose typed reads and guarded writes. Writes require retrieval, normalization, SHA-256 comparison, snapshot metadata, diff, approval, update, re-read verification, and audit. `appsscript.json` scope changes are security-sensitive.
