# Local development

Use the exact backend, extension, and unpacked-Chrome instructions in the product README. Local configuration comes from an ignored `.env`. Health and project-context validation require no external credentials. OAuth and provider-dependent work must use explicit mock adapters until developer credentials are configured outside the repository.
