# Local development

Use the exact backend and extension commands in the root product README. Local configuration comes from an ignored `.env`. Health checks require no external credentials. OAuth and provider-dependent work must use explicit mock adapters until developer credentials are configured outside the repository.
