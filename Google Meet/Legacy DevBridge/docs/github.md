# GitHub integration

The target architecture uses GitHub App installation tokens issued server-side. Repository, branch, content, commit, and pull-request calls will sit behind interfaces suitable for mocked tests. Protected branches are never bypassed.
