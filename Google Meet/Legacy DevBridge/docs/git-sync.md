# Git synchronization

Apps Script and GitHub files will normalize into a shared project-file model. The sync engine will classify identical, locally or remotely modified/added/deleted, and conflict states. Neither initial connection nor subsequent synchronization overwrites a conflict automatically.
