# Testing standards

- Unit-test hashing, normalization, sync classification, conflicts, stale writes, policies, mappings, onboarding, and provider boundaries as their milestones land.
- Mock network provider interfaces; integration tests must not depend on personal credentials.
